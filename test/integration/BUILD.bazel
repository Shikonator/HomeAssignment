load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "grpc_integration_test",
    size = "medium",
    srcs = ["grpc_integration_test.cc"],
    deps = [
        "//src/aggregator:engine",
        "//src/aggregator:service",
        "//proto/md/v1:bbo_cc_grpc",
        "//proto/md/v1:price_bands_cc_grpc",
        "//proto/md/v1:status_cc_grpc",
        "//proto/md/v1:volume_bands_cc_grpc",
        "@googletest//:gtest_main",
    ],
)
